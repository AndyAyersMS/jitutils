# Runtime dependencies. Prefer `pip install -e .` (uses pyproject.toml)
# unless you specifically need the exact pins below.
gymnasium>=0.29,<2.0
numpy>=1.24,<3.0
pydantic>=2.5,<3.0
pandas>=2.0
stable-baselines3[extra]>=2.3,<3.0
torch>=2.1
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65

# Development
pylint>=3.0
pytest>=7.4
